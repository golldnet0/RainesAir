<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ariad.rainesair">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".ContactActivity" />
        <activity android:name=".ForgotInfoActivity" />
        <activity
            android:name=".LoggedInActivity"
            android:label="@string/title_activity_logged_in"
            android:theme="@style/AppTheme" />
        <activity android:name=".RegisterActivity" />
        <activity
            android:name=".POActivity"
            android:label="@string/title_activity_po"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".SOActivity"
            android:label="@string/title_activity_so"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ROActivity"
            android:label="@string/title_activity_ro"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".PartsActivity"
            android:label="@string/title_activity_parts"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".InventoryActivity"
            android:label="@string/title_activity_inventory"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".CustomersActivity"
            android:label="@string/title_activity_customers"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".PersonalInfoActivity"
            android:label="@string/title_activity_personal_info"
            android:theme="@style/AppTheme" />
        <activity android:name=".CustomerAddActivity" />
        <activity android:name=".ChangePasswordActivity" />
        <activity
            android:name=".SettingsActivity"
            android:label="@string/title_activity_settings" />
        <activity android:name=".MenuActivityCustomer" />
        <activity android:name=".CustomersViewDetailsActivity" />
        <activity android:name=".PartsAddActivity" />
        <activity android:name=".PartsViewDetailsActivity" />
        <activity android:name=".InventoryAddActivity" />
        <activity android:name=".PublishedPartsMenuActivity" />
        <activity android:name=".POAddActivity" />
        <activity android:name=".MenuActivityPart" />
        <activity android:name=".aaron.ViewMessagesActivity" />
        <activity android:name=".aaron.ComposeMessageActivity" />
        <activity android:name=".aaron.ViewIndividualMessageActivity" />
        <activity android:name=".jc_payments.PaymentActivityPaypal" />
        <activity android:name=".jc_payments.ConfirmationActivity" />
        <activity android:name=".SOAddActivity" />
        <activity android:name=".ROAddActivity" />
        <activity android:name=".MenuActivityPO" />
        <activity android:name=".MenuActivitySO" />
        <activity android:name=".MenuActivityRO" />

        <uses-feature
            android:name="android.hardware.fingerprint"
            android:required="false" />

        <uses-permission android:name="android.permission.USE_FINGERPRINT" />

        <activity android:name=".POViewDetailsActivity" />
        <activity android:name=".ROViewDetailsActivity" />
        <activity android:name=".SOViewDetailsActivity" />
        <activity android:name=".MenuActivityInventory" />
        <activity android:name=".InventoryViewDetailsActivity" />
        <activity android:name=".aaron.RatingActivity" />
        <activity android:name=".aaron.ViewPartActivity"></activity>
    </application>

</manifest>